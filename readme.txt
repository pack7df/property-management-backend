Instruction to set up.

1. Create an sql express server
2. Go inside folder PropertyManagement.API and run "dotnet run seed". It creates DB schema and seed the values with samples.

The user sample credentials: 
username: userTest
password: 1234567

Check swagger to test.